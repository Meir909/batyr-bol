# 🎮 BATYR BOL — TASKS.md для Claude Code

## 📋 Обзор проекта

**"Батыр Бол"** — образовательная RPG-игра по истории Казахстана.
- **Стек:** Python Flask (бэкенд) + HTML5/Tailwind CSS/Vanilla JS (фронтенд)
- **База данных:** localStorage (фронтенд), JSON-файлы (сервер)
- **AI:** OpenAI GPT-4o-mini для генерации миссий
- **Деплой:** Render.com
- **Язык интерфейса:** Казахский (основной)
- **Авторизация:** Тестовый аккаунт (test@batyrbol.kz / batyr123)

---

## 🗂️ Структура проекта (целевая)

```
batyr-bol/
├── app.py                          # Главный Flask файл
├── requirements.txt                # Python зависимости
├── render.yaml                     # Конфиг деплоя Render.com
├── .env                            # Секреты (OPENAI_API_KEY, SECRET_KEY)
├── .env.example                    # Пример переменных окружения
├── .gitignore
├── data/
│   └── fallback_missions.json      # Резервные миссии (если AI недоступен)
├── static/
│   ├── css/
│   │   └── style.css               # Кастомные стили поверх Tailwind
│   ├── js/
│   │   ├── auth.js                 # Логика авторизации
│   │   ├── game.js                 # Главная игровая логика
│   │   ├── mission.js              # Логика миссий
│   │   ├── profile.js              # Профиль, уровни, XP
│   │   ├── clans.js                # Клановая система
│   │   ├── leaderboard.js          # Лидерборд
│   │   └── sounds.js               # Звуковые эффекты
│   ├── sounds/
│   │   ├── correct.mp3             # Правильный ответ
│   │   ├── wrong.mp3               # Неправильный ответ
│   │   ├── victory.mp3             # Победа в миссии
│   │   ├── defeat.mp3              # Поражение
│   │   ├── level_up.mp3            # Повышение уровня
│   │   └── background.mp3          # Фоновая музыка
│   └── images/
│       ├── ablai.png               # Портрет Абылай хана
│       ├── abai.png                # Портрет Абая
│       ├── aiteke.png              # Портрет Айтеке би
│       ├── heart_full.svg          # Жизнь (полная)
│       ├── heart_empty.svg         # Жизнь (пустая)
│       └── logo.png                # Логотип игры
└── templates/
    ├── intro.html                  # Страница входа/авторизации
    ├── igra.html                   # Главная страница игры
    ├── mission.html                # Страница миссии
    ├── profile.html                # Профиль игрока
    ├── clans.html                  # Страница кланов
    └── leaderboard.html            # Лидерборд
```

---

## 🚀 ФАЗЫ РАЗРАБОТКИ

---

## PHASE 1: Фундамент и авторизация

### TASK 1.1 — Настройка Flask приложения
**Файл:** `app.py`
**Приоритет:** 🔴 Критический

Настроить Flask приложение с базовой конфигурацией:

```python
# Что нужно реализовать:
# 1. Flask app с secret_key из .env
# 2. Session конфигурация (24 часа)
# 3. Rate limiting (flask-limiter) — 100 req/min
# 4. CORS если нужен
# 5. Все маршруты:
#    GET  /           → redirect на /intro если не залогинен, иначе /igra
#    GET  /intro      → intro.html
#    POST /login      → проверка test@batyrbol.kz / batyr123
#    GET  /logout     → очистка сессии, redirect на /intro
#    GET  /igra       → igra.html (требует авторизации)
#    GET  /mission    → mission.html (требует авторизации)
#    GET  /profile    → profile.html (требует авторизации)
#    GET  /clans      → clans.html (требует авторизации)
#    GET  /leaderboard → leaderboard.html (требует авторизации)
#    POST /api/mission/generate → генерация миссии через OpenAI
#    POST /api/clans/create     → создать клан
#    POST /api/clans/join       → вступить в клан
#    GET  /api/leaderboard      → получить топ игроков
#    POST /api/leaderboard/update → обновить счёт игрока
```

**Детали реализации:**
- Декоратор `@login_required` для защищённых маршрутов
- При неверном логине — flash сообщение на казахском: "Қате email немесе пароль"
- Session хранит: `user_id`, `email`, `last_activity`
- Auto-logout если `last_activity` > 24 часов
- `requirements.txt`: flask, openai, python-dotenv, flask-limiter, gunicorn

---

### TASK 1.2 — Страница входа (intro.html)
**Файл:** `templates/intro.html`
**Приоритет:** 🔴 Критический

Создать красивую страницу входа в казахском историческом стиле:

**Визуальный дизайн:**
- Тёмный фон (`#0a0a1a`) с золотыми акцентами (`#FFD700`)
- Крупный заголовок: **"БАТЫР БОЛ"** с эффектом свечения
- Подзаголовок на казахском: "Қазақстан тарихын үйрен"
- Анимированный фон: плавающие казахские орнаменты (CSS анимация)
- Форма входа по центру с полями email и пароль
- Кнопка "Кіру" (войти) — золотая с hover эффектом

**Функциональность:**
- POST запрос на `/login`
- Показ ошибок из Flask flash сообщений
- После успешного входа — редирект на `/igra`
- Подсказка с тестовыми данными мелким текстом внизу формы

**Анимации:**
- Fade-in при загрузке страницы (0.5s)
- Пульсация логотипа (2s infinite)
- Частицы/звёзды на фоне (JS canvas или CSS)

---

### TASK 1.3 — Файл конфигурации и переменные окружения
**Файлы:** `.env.example`, `render.yaml`, `.gitignore`
**Приоритет:** 🔴 Критический

**.env.example:**
```
OPENAI_API_KEY=your_openai_api_key_here
SECRET_KEY=your_secret_key_here
FLASK_ENV=production
```

**render.yaml:**
```yaml
services:
  - type: web
    name: batyr-bol
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: SECRET_KEY
        sync: false
```

**.gitignore:** включить `.env`, `__pycache__/`, `*.pyc`, `data/users/`

---

## PHASE 2: Главная страница игры

### TASK 2.1 — igra.html (главная игровая страница)
**Файл:** `templates/igra.html`
**Приоритет:** 🔴 Критический

Главная страница после авторизации. Здесь игрок видит своих персонажей и выбирает миссию.

**Структура страницы:**

**Хедер (фиксированный):**
- Логотип "БАТЫР БОЛ" слева
- Информация игрока справа: аватар + имя + уровень + XP полоска
- Кнопки навигации: Профиль | Кланы | Лидерборд | Выход

**Секция приветствия:**
- "Сәлем, Батыр! 👋" (имя берётся из localStorage)
- Текущий уровень и прогресс XP (визуальная полоска)
- Количество завершённых миссий

**Секция выбора персонажа (3 карточки):**

Каждая карточка содержит:
- Иллюстрация персонажа (изображение)
- Имя на казахском (крупно)
- Тип миссий (иконка + текст)
- Краткое описание (2-3 строки)
- Кнопка "Миссияны бастау" (начать миссию)

Карточки персонажей:
1. **Абылай хан** — ⚔️ Әскери миссиялар (Военные миссии)
   - "Батырлық пен стратегия туралы үйрен"
   - Стиль карточки: красно-золотой
2. **Абай** — 📚 Әдеби миссиялар (Литературные миссии)
   - "Поэзия мен даналық әлемін ашу"
   - Стиль карточки: синий-золотой
3. **Айтеке би** — ⚖️ Құқықтық миссиялар (Правовые миссии)
   - "Заң мен әділеттілік туралы үйрен"
   - Стиль карточки: зелёно-золотой

**Нижняя панель:**
- Последние достижения (3 иконки)
- Кнопка "Жетістіктер" (все достижения — модальное окно)

**Модальное окно выбора персонажа:**
- Открывается при клике "Миссияны бастау"
- Подтверждение выбора персонажа
- Кнопки: "Растау" (подтвердить) | "Болдырмау" (отмена)
- При подтверждении: сохранить в localStorage `selectedCharacter`, редирект на `/mission`

**JS логика (game.js):**
```javascript
// Что реализовать:
// 1. Загрузка данных из localStorage при старте
// 2. Отображение уровня и XP
// 3. Обработка выбора персонажа
// 4. Отображение достижений
// 5. Функция logout (очистка сессии + редирект)
```

---

## PHASE 3: Система миссий (ключевая механика)

### TASK 3.1 — API генерации миссий
**Файл:** `app.py` (маршрут `/api/mission/generate`)
**Приоритет:** 🔴 Критический

**POST /api/mission/generate**

Request body:
```json
{
  "character": "Абылай хан",
  "level": 3,
  "previousMissions": ["mission_id_1", "mission_id_2"]
}
```

Response:
```json
{
  "id": "mission_uuid",
  "character": "Абылай хан",
  "text_kz": "Исторический текст на казахском (150-200 слов)",
  "questions_kz": ["Вопрос 1?", "Вопрос 2?", "Вопрос 3?", "Вопрос 4?", "Вопрос 5?"],
  "options_kz": [
    ["Правильный ответ", "Неверный 1", "Неверный 2", "Неверный 3"],
    ["Правильный ответ", "Неверный 1", "Неверный 2", "Неверный 3"],
    ...
  ],
  "correct_answers": [0, 2, 1, 3, 0],
  "explanations_kz": ["Пояснение 1", "Пояснение 2", ...],
  "xp_reward": 35,
  "difficulty": "medium"
}
```

**Логика бэкенда:**

1. **Формирование промпта** в зависимости от персонажа и уровня:
   - Абылай хан → темы: Аныракай шайқасы, джунгарские войны, дипломатия
   - Абай → темы: поэзия, "Қара сөздер", просвещение, философия
   - Айтеке би → темы: "Жеті Жарғы", решение споров, обычное право

   Уровни 1-2: простые факты ("В каком году...")
   Уровни 3-5: анализ ("Почему хан решил...")
   Уровни 6-10: глубокое понимание ("Каковы последствия...")

2. **Системный промпт для OpenAI:**
```
Ты — эксперт по истории Казахстана. Создай образовательную миссию для игры "Батыр Бол".
Всё на казахском языке. Исторически точно.
Верни ТОЛЬКО валидный JSON без markdown, без пояснений.
```

3. **Валидация JSON ответа:**
   - Проверить все обязательные поля
   - Проверить что `correct_answers` содержит индексы 0-3
   - Проверить что 5 вопросов и 5 наборов ответов
   - При ошибке — retry (до 3 раз)

4. **Fallback:** если все попытки провалились — загрузить из `data/fallback_missions.json`

5. **Rate limiting:** не более 20 генераций в час с одного IP

---

### TASK 3.2 — Файл fallback миссий
**Файл:** `data/fallback_missions.json`
**Приоритет:** 🟡 Высокий

Создать по 3 готовые миссии для каждого персонажа (итого 9 миссий).

**Структура файла:**
```json
{
  "Абылай хан": [
    {
      "id": "ablai_fallback_1",
      "text_kz": "...(150+ слов про Аныракайскую битву)...",
      "questions_kz": ["5 вопросов"],
      "options_kz": [["4 варианта для каждого"]],
      "correct_answers": [0, 1, 2, 0, 3],
      "explanations_kz": ["5 пояснений"],
      "xp_reward": 25,
      "difficulty": "easy"
    },
    ...
  ],
  "Абай": [...],
  "Айтеке би": [...]
}
```

**Темы для Абылай хана:**
1. Аныракайская битва (1729) — разгром джунгар
2. Дипломатия с Россией — принятие подданства
3. Объединение трёх жузов

**Темы для Абая:**
1. Биография и детство в степи
2. "Қара сөздер" — слова-назидания
3. Влияние на казахское просвещение

**Темы для Айтеке би:**
1. "Жеті Жарғы" — семь уложений
2. Знаменитые судебные решения
3. Роль биев в казахском обществе

---

### TASK 3.3 — Страница миссии (mission.html)
**Файл:** `templates/mission.html`
**Приоритет:** 🔴 Критический

**Структура страницы:**

**Хедер миссии:**
- Имя персонажа + его иконка (слева)
- Шаг миссии: "1 / 5" (по центру)
- Жизни: ❤️❤️❤️ (справа, анимированные)

**Экран загрузки (показывается пока идёт запрос к AI):**
- Спиннер с казахским текстом: "Миссия жасалуда..."
- Анимация на 2-5 секунд

**Основная область миссии:**

Шаг 1 — Исторический текст:
- Карточка с историческим текстом (150-200 слов)
- Иллюстрация персонажа сбоку
- Кнопка "Дайынмын!" (Готов!) → переход к вопросам

Шаги 2-6 — Вопросы (по 1 вопросу):
- Номер вопроса и прогресс-бар (шаг X из 5)
- Текст вопроса (крупный)
- 4 кнопки-варианта ответа (A, B, C, D)
- При клике:
  - Правильно: кнопка зеленеет, звук correct.mp3, показать пояснение на 1.5с, переход к следующему
  - Неправильно: кнопка краснеет, вибрация, звук wrong.mp3, правильная подсвечивается зелёным, показать пояснение, -1 жизнь

**Экран победы:**
- Анимация: золотые частицы, звук victory.mp3
- "Жеңіс! 🏆" (крупно)
- Заработанный XP: "+35 XP"
- Если новый уровень — анимация level_up.mp3 + "Жаңа деңгей! ⭐"
- Полученные достижения (если есть)
- Кнопки: "Тағы ойна" (ещё раз) | "Басты бетке" (на главную)

**Экран поражения:**
- "Жеңіліс 💔"
- Сколько вопросов ответил правильно
- Кнопки: "Қайта ойна" (переиграть) | "Басты бетке"

**JS логика (mission.js):**
```javascript
// Что реализовать:
// 1. Fetch POST /api/mission/generate при загрузке
// 2. Управление состоянием: currentStep, lives, answers
// 3. Отображение вопросов и обработка ответов
// 4. Восстановление жизни: +1 за 2 правильных ответа подряд
// 5. Подсчёт XP: base(20) + bonus_per_correct(5) + perfect_bonus(10)
// 6. Обновление localStorage: XP, уровень, достижения, история миссий
// 7. Анимации победы/поражения
// 8. Проверка и разблокировка достижений
```

---

## PHASE 4: Профиль и прогрессия

### TASK 4.1 — Система уровней и XP (profile.js)
**Файл:** `static/js/profile.js`
**Приоритет:** 🟡 Высокий

**localStorage структура (полная):**
```javascript
{
  // Базовая информация
  "playerName": "Батыр",
  "playerLevel": 1,
  "experience": 0,
  "totalMissions": 0,
  "perfectMissions": 0,      // миссии без потери жизней

  // История миссий
  "missionHistory": [
    {
      "id": "uuid",
      "character": "Абылай хан",
      "completed": true,
      "perfect": false,
      "xpEarned": 35,
      "timestamp": "ISO string",
      "correctAnswers": 4,
      "totalQuestions": 5
    }
  ],

  // Персонажи
  "selectedCharacter": "Абылай хан",
  "unlockedCharacters": ["Абылай хан", "Абай", "Айтеке би"],

  // Достижения
  "achievements": [],

  // Клан
  "clanName": null,
  "clanRole": null,           // "leader" | "member"

  // Настройки
  "soundEnabled": true,
  "musicEnabled": true,
  "musicVolume": 0.3          // 0.0 - 1.0
}
```

**Функции для реализации:**
```javascript
function calculateLevel(xp)          // Каждые 100 XP = новый уровень (макс 10)
function addXP(amount)               // Добавить XP, проверить level up
function checkAchievements()         // Проверить все достижения
function getXPForNextLevel(level)    // XP до следующего уровня
function getLevelTitle(level)        // Казахское название уровня
```

**Названия уровней на казахском:**
- 1: Жасөспірім (Новичок)
- 2: Оқушы (Ученик)
- 3: Батыр (Герой)
- 4: Жауынгер (Воин)
- 5: Қолбасшы (Командир)
- 6: Би (Судья)
- 7: Хан (Хан)
- 8: Данышпан (Мудрец)
- 9: Бекзат (Аристократ)
- 10: Ұлы батыр (Великий герой)

---

### TASK 4.2 — Страница профиля (profile.html)
**Файл:** `templates/profile.html`
**Приоритет:** 🟡 Высокий

**Секции страницы:**

**Карточка игрока:**
- Большой аватар (генерируется по первой букве имени, золотой круг)
- Имя + уровень + казахский титул уровня
- XP полоска с цифрами (X / 100 XP)
- Статистика: Миссий пройдено | Правильных ответов % | Идеальных миссий

**Раздел достижений:**
Все достижения отображаются в сетке:

| ID | Иконка | Название (KZ) | Условие |
|-----|--------|---------------|---------|
| `first_mission` | 🏅 | Бірінші миссия | Пройти 1 миссию |
| `five_in_a_row` | 🔥 | 5 миссия қатарынан | 5 миссий подряд без поражения |
| `ten_correct` | ✅ | 10 дұрыс жауап | 10 правильных ответов суммарно |
| `level_5` | ⭐ | 5-ші деңгей шебері | Достичь 5 уровня |
| `historian` | 📚 | Тарихшы | Пройти миссии всех 3 персонажей |
| `perfect_mission` | 💎 | Мінсіз миссия | Пройти миссию без потери жизней |
| `clan_member` | 🏰 | Клан мүшесі | Вступить в клан |
| `century_xp` | 💯 | 100 XP | Набрать 100 XP |

Незаработанные достижения отображаются серыми с замком 🔒

**История миссий:**
- Список последних 10 миссий
- Каждая запись: персонаж + дата + XP + результат (✅/❌) + правильных ответов

---

## PHASE 5: Клановая система

### TASK 5.1 — Клановая система (бэкенд)
**Файл:** `app.py` + `data/clans.json`
**Приоритет:** 🟠 Средний

**Хранение кланов** (`data/clans.json`):
```json
{
  "clans": [
    {
      "id": "clan_uuid",
      "name": "Жасұлан",
      "description": "Молодые воины",
      "leader": "session_user_id",
      "members": ["id1", "id2"],
      "memberNames": {"id1": "Батыр", "id2": "Арман"},
      "totalXP": 350,
      "createdAt": "ISO string"
    }
  ]
}
```

**API эндпоинты:**

`POST /api/clans/create`
- Тело: `{"name": "Жасұлан", "description": "..."}`
- Создаёт клан, делает создателя лидером
- Сохраняет в localStorage: `clanName`, `clanRole: "leader"`
- Ограничение: 1 клан на игрока

`POST /api/clans/join`
- Тело: `{"clanId": "uuid"}`
- Присоединяет к клану (макс 50 игроков)
- Обновляет localStorage

`GET /api/clans/list`
- Возвращает список всех кланов с топ-3 участниками

`POST /api/clans/update_xp`
- Обновляет totalXP клана при завершении миссии

---

### TASK 5.2 — Страница кланов (clans.html)
**Файл:** `templates/clans.html`
**Приоритет:** 🟠 Средний

**Если игрок не в клане:**
- Секция "Клан құру" (Создать клан): форма с именем и описанием
- Список существующих кланов с кнопками "Қосылу" (Вступить)
- Каждый клан: имя + кол-во участников + суммарный XP + топ-3 игрока

**Если игрок в клане:**
- Карточка своего клана (имя, описание, позиция в рейтинге)
- Список участников с XP каждого
- Суммарный XP клана
- Если лидер — кнопка "Кланды жою" (Удалить клан)
- Кнопка "Кланнан шығу" (Покинуть клан)

---

## PHASE 6: Лидерборд

### TASK 6.1 — Лидерборд (бэкенд)
**Файл:** `app.py` + `data/leaderboard.json`
**Приоритет:** 🟡 Высокий

**Хранение** (`data/leaderboard.json`):
```json
{
  "players": [
    {
      "sessionId": "...",
      "name": "Батыр",
      "level": 5,
      "totalXP": 450,
      "missionsCompleted": 12,
      "clanName": "Жасұлан",
      "lastUpdated": "ISO string"
    }
  ]
}
```

**POST /api/leaderboard/update**
- Вызывается после каждой завершённой миссии
- Тело: `{"name": "...", "xp": 450, "level": 5, "missions": 12, "clan": "..."}`
- Обновляет или создаёт запись по sessionId

**GET /api/leaderboard**
- Возвращает топ-50 игроков отсортированных по totalXP
- Query param: `?type=players` (по умолчанию) или `?type=clans`

---

### TASK 6.2 — Страница лидерборда (leaderboard.html)
**Файл:** `templates/leaderboard.html`
**Приоритет:** 🟡 Высокий

**Структура:**
- Табы: "Ойыншылар" (Игроки) | "Кландар" (Кланы)
- Текущий игрок подсвечен (его позиция и строка)

**Таблица игроков:**
- Место (1🥇 2🥈 3🥉 4+ обычно)
- Имя игрока
- Уровень + казахский титул
- Клан (если есть)
- Суммарный XP

**Таблица кланов:**
- Место
- Название клана
- Количество участников
- Суммарный XP клана

---

## PHASE 7: Звуки и анимации

### TASK 7.1 — Система звуков (sounds.js)
**Файл:** `static/js/sounds.js`
**Приоритет:** 🟠 Средний

**Реализовать SoundManager класс:**
```javascript
class SoundManager {
  constructor()
  // Методы:
  playCorrect()       // correct.mp3 — правильный ответ
  playWrong()         // wrong.mp3 — неправильный ответ
  playVictory()       // victory.mp3 — победа
  playDefeat()        // defeat.mp3 — поражение
  playLevelUp()       // level_up.mp3 — новый уровень
  startBGMusic()      // background.mp3 loop, volume 0.3
  stopBGMusic()
  toggleSound()       // вкл/выкл все звуки
  toggleMusic()       // вкл/выкл фоновую музыку
  setMusicVolume(v)   // 0.0 - 1.0
  // Настройки берутся из localStorage
}

// Глобальный экземпляр
const soundManager = new SoundManager();
```

**Кнопки управления звуком:**
- Добавить в хедер всех страниц: 🔊/🔇 (звук) и 🎵/🎵̶ (музыка)
- Состояние сохраняется в localStorage

**Важно:** Автовоспроизведение музыки только после первого взаимодействия пользователя (требование браузеров).

---

### TASK 7.2 — CSS анимации (style.css)
**Файл:** `static/css/style.css`
**Приоритет:** 🟠 Средний

**Реализовать следующие анимации:**

```css
/* 1. Анимация появления страницы */
@keyframes fadeIn { from {opacity:0; transform:translateY(20px)} to {opacity:1; transform:translateY(0)} }

/* 2. Пульсация золотым (победа, достижения) */
@keyframes goldPulse { 0%,100%{box-shadow:0 0 10px #FFD700} 50%{box-shadow:0 0 30px #FFD700, 0 0 60px #FFD700} }

/* 3. Вибрация (неправильный ответ) */
@keyframes shake { 0%,100%{transform:translateX(0)} 25%{transform:translateX(-10px)} 75%{transform:translateX(10px)} }

/* 4. Отскок (правильный ответ) */
@keyframes bounce { 0%,100%{transform:scale(1)} 50%{transform:scale(1.05)} }

/* 5. Плавающие частицы для победного экрана */
@keyframes float { 0%{opacity:1;transform:translateY(0)} 100%{opacity:0;transform:translateY(-100px)} }

/* 6. Анимация XP полоски */
@keyframes xpFill { from{width:var(--from)} to{width:var(--to)} }

/* 7. Мигание жизней при потере */
@keyframes heartLose { 0%,100%{opacity:1} 50%{opacity:0} }
```

**Цветовая система (CSS переменные):**
```css
:root {
  --bg-primary: #0a0a1a;
  --bg-secondary: #12122a;
  --bg-card: #1a1a35;
  --gold: #FFD700;
  --gold-dark: #B8860B;
  --gold-glow: rgba(255, 215, 0, 0.3);
  --red-life: #E74C3C;
  --green-correct: #2ECC71;
  --blue-wisdom: #3498DB;
  --text-primary: #FFFFFF;
  --text-secondary: #B0B0C0;
}
```

---

## PHASE 8: Деплой и финальная настройка

### TASK 8.1 — Конфигурация для Render.com
**Файл:** `render.yaml` + финальная проверка
**Приоритет:** 🟡 Высокий

**Чеклист перед деплоем:**
- [ ] Все переменные окружения задокументированы в `.env.example`
- [ ] `requirements.txt` актуален (flask, openai, gunicorn, python-dotenv, flask-limiter)
- [ ] `gunicorn` настроен: `gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT`
- [ ] Статические файлы раздаются Flask правильно
- [ ] Все пути к файлам используют `os.path.join` (не хардкод `/`)
- [ ] `data/` директория создаётся автоматически если не существует
- [ ] Rate limiting работает корректно
- [ ] 404 и 500 ошибки обрабатываются красиво

**render.yaml:**
```yaml
services:
  - type: web
    name: batyr-bol
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
    disk:
      name: data
      mountPath: /data
      sizeGB: 1
```

---

### TASK 8.2 — Responsive дизайн
**Приоритет:** 🟠 Средний

**Контрольные точки (breakpoints):**
- Mobile: < 640px
- Tablet: 640px - 1024px
- Desktop: > 1024px

**Что адаптировать для мобильных:**
- Карточки персонажей: 3 колонки → 1 колонка
- Хедер: полная навигация → гамбургер меню
- Кнопки ответов: ширина 100%
- Размер шрифтов: уменьшить на 10-15%
- Лидерборд: скрыть часть колонок
- Модальные окна: полный экран на мобайл

**Tailwind классы для использования:**
```html
<!-- Пример адаптивной сетки карточек -->
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
```

---

## 📊 Сводная таблица задач

| Task | Название | Приоритет | Статус |
|------|----------|-----------|--------|
| 1.1 | Flask приложение + маршруты | 🔴 Крит | ⬜ |
| 1.2 | Страница входа (intro.html) | 🔴 Крит | ⬜ |
| 1.3 | Конфиги (.env, render.yaml) | 🔴 Крит | ⬜ |
| 2.1 | Главная игры (igra.html) | 🔴 Крит | ⬜ |
| 3.1 | API генерации миссий (OpenAI) | 🔴 Крит | ⬜ |
| 3.2 | Fallback миссии (JSON) | 🟡 Высок | ⬜ |
| 3.3 | Страница миссии (mission.html) | 🔴 Крит | ⬜ |
| 4.1 | Система XP и уровней (JS) | 🟡 Высок | ⬜ |
| 4.2 | Страница профиля | 🟡 Высок | ⬜ |
| 5.1 | Кланы (бэкенд) | 🟠 Средн | ⬜ |
| 5.2 | Страница кланов | 🟠 Средн | ⬜ |
| 6.1 | Лидерборд (бэкенд) | 🟡 Высок | ⬜ |
| 6.2 | Страница лидерборда | 🟡 Высок | ⬜ |
| 7.1 | Звуки (sounds.js) | 🟠 Средн | ⬜ |
| 7.2 | CSS анимации | 🟠 Средн | ⬜ |
| 8.1 | Деплой на Render.com | 🟡 Высок | ⬜ |
| 8.2 | Responsive дизайн | 🟠 Средн | ⬜ |

---

## 🎯 MVP порядок выполнения

Рекомендуемый порядок для быстрого MVP:

```
1.3 → 1.1 → 1.2 → 2.1 → 3.1 → 3.2 → 3.3 → 4.1 → 6.1 → 6.2 → 8.1
```

После MVP:
```
4.2 → 5.1 → 5.2 → 7.1 → 7.2 → 8.2
```

---

## 🔑 Ключевые технические решения

**Почему localStorage а не БД:**
- Render.com free tier не гарантирует персистентность данных между перезапусками
- Лидерборд и кланы хранятся в JSON файлах через disk mount (`/data`)
- Прогресс игрока — в localStorage браузера

**Почему только тестовый аккаунт:**
- Простота для MVP
- Любой может попробовать игру без регистрации
- В Phase 2 добавить реальную регистрацию

**OpenAI API оптимизация:**
- Использовать `gpt-4o-mini` (дешевле, достаточно для вопросов)
- Температура: 0.7 (баланс точности и разнообразия)
- max_tokens: 1500 (достаточно для структуры миссии)
- Кэшировать успешные миссии — не регенерировать одну тему дважды для одного игрока

---

## 📝 Примечания для Claude Code

- Весь текст интерфейса на **казахском языке**
- Исторические факты должны быть **точными** (проверять с промптом)
- Тёмная тема везде, золотые акценты — фирменный стиль
- Анимации плавные, не раздражающие (duration 300-500ms)
- При любой ошибке API — graceful fallback, не белый экран
- Фоновая музыка только после клика пользователя (автовоспроизведение заблокировано браузерами)
- Все даты/время хранить в UTC ISO format
